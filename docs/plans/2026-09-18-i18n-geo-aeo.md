# Plan 2026-09-18 — Full 9 Locales + Services GEO/AEO Enrichment

## Quyết định đã chốt
- (a) `services.image_alt` có — AI tìm ảnh auto bổ sung alt song ngữ + Việt hoá GEO
- (b) Full 9 subdirectories `/vi`, `/en`, `/ko`, `zh-CN`, `ja`, `th`, `fr`, `de`, `es` — effort gộp PR1+PR2
- GEO/AEO: thêm `speakable` + `HowTo` cho service
- Backfill cho phép ghi đè `image_alt` rỗng
- Domain chuẩn `https://minhair.vercel.app`, redirect 308 từ `min-nail-hair.vercel.app` và `minnailhair.vn`

## Kiến trúc
- `proxy.ts` — locale prefix rewrite (`/{locale}/path` -> `/path` + set `locale` cookie + `x-locale` header), domain canonical redirect, auth path normalize
- `app/layout.tsx` — đọc `x-locale` header ưu tiên, `alternates.languages` 9 + `x-default` -> `/vi`
- `app/sitemap.ts` — `withHreflang()` sinh `<xhtml:link>` cho 9 locales + services (slugify), `revalidate=43200`
- `public/robots.txt` + `vercel.json` redirects/headers

## Dịch vụ thiếu hình/nội dung
- `lib/service-enricher.ts` — `enrichServiceImage()` via `searchImages()` (song ngữ) + `enrichServiceDescription()` via Gemini (120-180 ký tự, chứa Lavita Charm/Thủ Đức)
- `app/admin/actions/services.ts` `saveService()` — nếu `image_url` rỗng -> enrich ảnh, nếu `description` <30 ký tự -> enrich mô tả, alt enforce GEO
- `lib/cache.ts` select thêm `image_alt` để homepage/display không rỗng
- `scripts/backfill-services.ts` — 1 lần backfill alt/img/desc cho services cũ
- `app/page.tsx:389` alt fallback GEO, `app/dich-vu/[slug]` HowTo 5 bước chà gót / 3 bước gội dưỡng sinh + speakable

## Cập nhật 2026-09-18 (bổ sung theo yêu cầu)
- **AutoSEO:** `lib/auto-seo.ts:6` `ARTICLE_SYSTEM` bổ sung quy tắc H1/H2/H3 chuẩn (H1=title, >=3 H2, mỗi H2 1-2 H3, không nhảy cấp), bố cục sapo + bullet/blockquote/bảng, review website (truyền `services` + `blogs` thật vào prompt để AI không bịa + tạo backlink), backlink nội bộ 2-3 link `[anchor](/dich-vu/slug)` trong `ensureHeadingStructureAndBacklinks()`.
- **Migration đã apply:** `SELECT image_alt` tồn tại, đã chạy SQL fill 23 alt + `npx tsx scripts/backfill-services.ts` fill 13 image_url + 19 description (Gemini quota fallback template GEO khi 429).
- **PR3 GEO/AEO:** `components/ArticleSchema.tsx:26` thêm `inLanguage` + `speakable` (h1, h2, .prose p:first-of-type), `proxy.ts:12` thêm `X-Robots-Tag: noindex` cho preview host, `vercel.json` sửa redirects 308.

## Đồng bộ lần sau
- Migration: `supabase/migrations/20250919000001_add_services_image_alt.sql`
- Helpers: `lib/image-alt.ts`, `lib/service-enricher.ts`
- Pipeline: `lib/image-search.ts` (enrichAlts), `lib/auto-seo.ts` (ensureGeoAlt + H1/H2/H3 + backlink), `app/blog/actions.ts` (enforcedAlt)
- Docs: `docs/Log.md`, `docs/Audit.md`, `docs/UPGRADE_PLAN.md`, plan này
