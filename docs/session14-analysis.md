# Session 14 Analysis for Big Pickle
> Generated by DeepSeek V4 Flash Free — 19/06/2026

## P4.13 — Accessibility (23 issues across 15 files)

### 1. Icon buttons without aria-label (13 issues)

| # | File | Line | Element | Fix |
|---|------|------|---------|-----|
| 1 | `components/AppointmentDetailModal.tsx` | 75 | Close button `<X>` | `aria-label="Đóng modal"` |
| 2 | `components/AppointmentDetailModal.tsx` | 207-212 | Edit button `<Edit>` | `aria-label="Chỉnh sửa thông tin đơn hàng"` |
| 3 | `components/AppointmentDetailModal.tsx` | 214-219 | Delete button `<Trash2>` | `aria-label="Hủy lịch hẹn"` |
| 4 | `components/BookingMascotGuide.tsx` | 59-67 | Dismiss button `<X>` | `aria-label="Đóng hướng dẫn"` |
| 5 | `components/AppointmentLookup.tsx` | 490-496 | Clear search `<X>` | `aria-label="Xóa tìm kiếm"` |
| 6 | `components/MasterSchedule.tsx` | 523-528 | Grid view `<Grid3x3>` | `aria-label="Xem dạng lưới"` |
| 7 | `components/MasterSchedule.tsx` | 530-536 | List view `<List>` | `aria-label="Xem dạng danh sách"` |
| 8 | `app/admin/components/TabStaff.tsx` | 313-319 | Detail chevron `<ChevronRight>` | `aria-label="Xem chi tiết nhân viên"` |
| 9 | `app/admin/components/TabPackages.tsx` | 57-62 | Edit button `<PenTool>` | `aria-label="Sửa gói"` |
| 10 | `app/admin/components/TabPackages.tsx` | 64-71 | Delete button `<XIcon>` | `aria-label="Xóa gói"` |
| 11 | `app/admin/page.tsx` | 132-137 | Menu hamburger `<MenuIcon>` | `aria-label="Mở menu"` |
| 12 | `app/admin/page.tsx` | 125-131 | Home link `<Home>` | `aria-label="Về trang chủ"` |
| 13 | `app/admin/page.tsx` | 253-259 | Home link sidebar `<Home>` | `aria-label="Về trang chủ"` |

### 2. Missing keyboard handlers on clickable divs (8 issues)

| # | File | Line | Element | Fix |
|---|------|------|---------|-----|
| 1 | `components/AppointmentDetailModal.tsx` | 67 | Backdrop `<div onClick>` | Add `role="presentation"` + `onKeyDown` Escape handler |
| 2 | `components/ScheduleDndComponents.tsx` | 21 | Clickable `<div>` | Add `role="button" tabIndex={0} onKeyDown` |
| 3-8 | `app/admin/components/TabStaff.tsx` | 244,269,272,275,278,281 | 6x `<td onClick>` | Add `role="button" tabIndex={0} onKeyDown` |

### 3. Missing focus trap in modals (7 issues)

| # | File | Line | Fix |
|---|------|------|-----|
| 1 | `components/LoadingOverlay.tsx` | 17-28 | Add `role="dialog" aria-modal="true"` + trap focus |
| 2 | `components/AppointmentDetailModal.tsx` | 67 | Add `role="dialog" aria-modal="true"` + trap focus + Escape handler |
| 3 | `app/admin/components/AddStaffModal.tsx` | 32 | Same |
| 4 | `app/admin/components/EditStaffModal.tsx` | 53 | Same |
| 5 | `app/admin/components/ServiceModal.tsx` | 67 | Same |
| 6 | `app/admin/components/StaffDetailModal.tsx` | 5 | Same |
| 7 | `app/admin/components/EditPackageModal.tsx` | ~4-7 | Same |

### 4. Missing skip link (1 global issue)

**File:** `app/layout.tsx` — No "Skip to content" link at top of `<body>`.
**Fix:** Add `<SkipLink />` component as first child of `<body>`.

### 5. Input without label (1 issue)

**File:** `components/MasterSchedule.tsx` line 451-460 — Date `<input>` with no `<label>` or `aria-label`.
**Fix:** `aria-label="Chọn ngày"`.

**Action for you:** Create `components/SkipLink.tsx`, `hooks/useFocusTrap.ts`, then fix all 23 issues inline.

---

## P4.14 — Social Proof & Micro-interactions

### Current state:
- **sonner** is installed (`"sonner": "^2.0.7"`) but only used in `app/admin/blog/page.tsx`
- `<Toaster>` only exists in blog page — **NOT in root layout**
- **3 custom in-house toast systems** exist (staff page, TabDashboard, staff random alert) — inconsistent
- **19 native `alert()`/`confirm()`** calls scattered across admin components
- **~15 div-based inline messages** (error/success banners without auto-dismiss)

### What to do:

1. **Add `<Toaster>` to root layout** (`app/layout.tsx`) — global toast container
2. **Replace 19 native alert()/confirm()** with `toast.success()` / `toast.error()`:
   - `app/staff/page.tsx:729,733,2614,2637,2688`
   - `app/admin/blog/page.tsx:340`
   - `app/admin/components/TabServices.tsx:12,17,20,23`
   - `app/admin/components/TabPackages.tsx:12,17,20,23`
   - `app/admin/components/TabStaff.tsx:289,295`
   - `app/admin/components/EditStaffModal.tsx:140`
   - `app/admin/components/TabSEO.tsx:291`
   - `app/admin/components/TodayMonitoringWidget.tsx:32`
3. **Replace div-based inline errors with sonner** in:
   - `app/booking/page.tsx:393-397` (errorMsg)
   - `app/login/page.tsx:89-109` (errorMsg/authError)
4. **Micro-interactions:**
   - Notification bell badge: add `animate-bounce` on increment
   - Status badges in MasterSchedule: add smooth color transition via `transition-colors duration-300`
   - Dashboard counters: animate on real-time data refresh

---

## P4.15 — Optimistic Booking UI

### Booking page (`app/booking/page.tsx:288-335`)

**Current:** Awaits full server round-trip before showing success (step 3).
**Fix:** Show step 3 immediately after validation, fire `submitBooking` in background. Add retry banner on step 3 if server fails.

```tsx
// Replace await pattern with optimistic:
setStep(3);
submitBooking(payload).then(res => {
  if (res.success) {
    invalidateCustomerCache(phone);
    localStorage.setItem('min_salon_customer_phone', phone);
    if (res.customerId) localStorage.setItem('min_salon_customer_id', res.customerId);
  } else {
    setErrorMsg('Có lỗi xảy ra: ' + res.error + ' — Vui lòng thử lại.');
  }
}).catch(err => {
  setErrorMsg('Lỗi kết nối: ' + err.message);
}).finally(() => {
  setIsSubmitting(false);
});
```

### Staff page optimistic updates (5 handlers):

| Handler | File:Line | Optimistic strategy |
|---------|-----------|---------------------|
| `handleCheckIn` | `app/staff/page.tsx:336-343` | Immediately set local attendance, fire `checkIn()` in background |
| `handleStartAppointment` | `app/staff/page.tsx:859-863` | Immediately set appt status to `IN_PROGRESS`, fire in background |
| `handleTakeRandom` | `app/staff/page.tsx:865-869` | Move appt from random to my-appointments locally, fire in background |
| `completeAppointment` | `app/staff/page.tsx:702-739` | Close modal, set `COMPLETED` immediately, fire in background, then show QR/review |
| `swapAppointment` | `app/staff/page.tsx:777-782` | Close modal, update staff_id locally, fire in background |

---

## P4.16 — Large Screen Optimization (>1440px)

### Config changes (`tailwind.config.ts`):
- No custom breakpoints exist. Add `xxl: '1600px'`.
- Add `container` center config.

### Layout changes (`app/layout.tsx`):
- Wrap `{children}` in `<div className="max-w-[1600px] mx-auto">`.

### Grid column expansions (add `xl:` / `xxl:` variants):

| File | Line | Current | Add |
|------|------|---------|-----|
| `app/page.tsx` | 261, 368 | `lg:grid-cols-3` | `xl:grid-cols-4` |
| `app/page.tsx` | 556 | `md:grid-cols-3` | `xl:grid-cols-4 xxl:grid-cols-6` |
| `components/MasterScheduleList.tsx` | 367 | `lg:grid-cols-2` | `xl:grid-cols-3` |
| `app/booking/page.tsx` | 683 | `sm:grid-cols-2` | `lg:grid-cols-3 xl:grid-cols-4` |

### Max-width increases:

| File | Line | Current | New |
|------|------|---------|-----|
| `app/page.tsx` | 170, 200, 458, 472, 499 | `max-w-3xl` (768px) / `max-w-4xl` (896px) | `max-w-4xl` / `max-w-5xl` |
| `app/page.tsx` | 205, 227, 245, 506, 519, 556 | `max-w-6xl` (1152px) | `max-w-7xl` (1280px) |
| `app/admin/page.tsx` | 338 | `max-w-6xl` | `max-w-7xl` |
| `app/booking/page.tsx` | 362-363 | `max-w-5xl` (1024px) | `max-w-7xl` (1280px) |

---

## P4.25 — React.memo Grid/List

### Current state: Zero `React.memo` usage in codebase.

### Priority order:

| Priority | File | Action |
|----------|------|--------|
| **P0** | `components/ScheduleDndComponents.tsx` | Wrap all 3 exports: `DraggableApptCard`, `DroppableSlotCell`, `DroppableStaffCard` with `React.memo` |
| **P1** | `components/MasterScheduleGrid.tsx` | Wrap default export with `React.memo` |
| **P1** | `components/MasterScheduleList.tsx` | Wrap all 3: `MasterScheduleList`, `TimeGroupedView`, `StaffGroupedView` with `React.memo`. Add `useMemo` for `filteredStaffList`, `renderedSlots` |
| **P2** | `components/BookingCalendar.tsx` | Wrap default with `React.memo`. Extract `TimeSlotButton = React.memo(...)` for slot buttons |
| **P3** | `components/MasterSchedule.tsx` | Wrap 5 callback props in `useCallback` so `React.memo` on children is effective: `getSlotAppointment`, `getSlotLock`, `getStatusStyle`, `isCascadeShifted`, `handleSelectAppt` |

---

## Summary for Pickle

**Total work items:** ~50 file edits across ~20 files
**Estimated time:** ~85 minutes (P4.13: 25m, P4.14: 15m, P4.15: 20m, P4.16: 15m, P4.25: 10m)
**Order:** P4.13 a11y first (foundation) → P4.14 toast + micro (UX layer) → P4.15 optimistic (booking UX) → P4.16 large screen (layout) → P4.25 memo (perf)
