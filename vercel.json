{
  "ignoreCommand": "git diff --quiet HEAD~1 -- '*.md' 'docs/**' '.agents/**' '.github/**' LICENSE 2>/dev/null && exit 0 || exit 1",
  "crons": [
    {
      "path": "/api/cron/seo-publish",
      "schedule": "0 13 * * 4"
    }
  ],
  "env": {
    "SHARP_IGNORE_GLOBAL_LIBVIPS": "1"
  }
}
